#include <stdio.h>
/**
 * main - A program that prints a line using printf function
 * Return: 0 (if it returns 0 that means it a success)
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art;\n")
		return (0);
}
